# 如何貢獻新競賽資料

感謝你願意幫助維護這個競賽資料庫！  
貢獻方式非常簡單，只需要修改一個 JSON 檔案。

---

## 🚀 最快的方式：直接在 GitHub 網頁上編輯

不需要安裝任何工具，直接在瀏覽器操作：

1. 前往本 Repo 的 [`competitions.json`](./competitions.json)
2. 點擊右上角的 ✏️ **鉛筆編輯圖示**
3. 在陣列最後加入新競賽資料（參考下方範本）
4. 點擊 **Commit changes**，填寫說明（例如：`新增：XXX競賽`）
5. 選擇 **Create a new branch and start a pull request**
6. 提交 PR，管理員會執行 `python3 sync.py` 同步資料後合併上線

---

## 📋 新增競賽範本

複製以下 JSON，填入實際資料後加入 `competitions.json`：

```json
{
  "id": 999,
  "name": "競賽完整名稱",
  "scope": "國內",
  "domains": ["AI工具應用"],
  "level": ["國中", "高中"],
  "organizer": "主辦單位",
  "description": "競賽描述（50–150 字）：說明競賽目的、適合什麼樣的學生、有哪些組別或特色。",
  "schedule": "重要時程，例如：報名：每年 9 月；初賽：11 月；決賽：12 月",
  "deadline": "報名截止日，例如：2026/11/30",
  "url": "https://官方網站",
  "tags": ["標籤1", "標籤2", "標籤3"],
  "highlight": false,
  "status": "即將開放",
  "updated": "2026-03"
}
```

### ✅ 欄位填寫規則

**`id`** — 唯一整數，不要與現有 ID 重複  
　　建議：國內賽用 100–199，國際賽用 200–299，以此類推

**`scope`** — 只能填以下三個值之一：
```
"國內"
"國內（接軌國際）"
"國際"
```

**`domains`** — 陣列，可選多個，只能從以下清單選：
```
"AI工具應用"   "數學"        "語文/文學"   "視覺藝術"
"科學研究"      "STEM"        "跨域創新"    "社會議題"
"教育科技"      "科技"
```
如果有新領域，請在 PR 說明中提出，管理員會討論是否新增。

**`level`** — 陣列，可選多個：
```
"國中"    "高中"
```

**`status`** — 只能填以下四個值之一：
```
"報名中"    "開放中"    "即將開放"    "已截止"
```

**`highlight`** — 是否標示為重點推薦：
- `true`：會顯示「★ 重點推薦」標籤（建議只用於知名度高或門檻低、值得廣泛推廣的競賽）
- `false`：一般競賽

**`updated`** — 你填寫資料的年月，格式 `"YYYY-MM"`

---

## 📝 開 Issue 回報問題

不確定如何填寫？直接 [開一個 Issue](https://github.com/YOUR_GITHUB_ACCOUNT/taiwan-k12-competition-tracker/issues/new) 告訴我們：
- 競賽名稱
- 官方網站連結
- 你認為適合的學段與領域

管理員會協助整理並加入。

---

## 🔄 更新現有競賽資料

如果發現資料有誤或過期，同樣可以：
1. 直接編輯 `competitions.json`
2. 修改有問題的欄位
3. **記得把 `"updated"` 欄位改成今天的年月**
4. Commit 說明寫：`更新：XXX競賽 截止日修正` 或 `更新：XXX競賽 2027 版本`

---

感謝每一位協助維護的老師與教育工作者！🙏
